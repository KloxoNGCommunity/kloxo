#!/bin/sh

if [ "$#" == 0 ] ; then
	echo
	echo " ---------------------------------------------------------------------------"
	echo "  format: sh $0 <module_name>"
	echo " ---------------------------------------------------------------------------"
	echo " - Because php53s using php 5.3.x, better use the same version"
	echo "   Example: echo 'sh $0 php53-xcache'"
	echo
	echo " - Certain modules need 'extension=/opt/php53s/lib/xxx.so'"
	echo "   rather than 'extension=xxx.so' inside their ini file on"
	echo "   '/opt/php53s/etc/php.d')"
	echo
	echo " - Check '/opt/php53s/error.log' after testing with 'php53s -m'"
	echo
	exit;
fi

yum install yum-utils -y

rm -rf /tmp/php53s-module/* >/dev/null 2>$1

mkdir -p /tmp/php53s-module >/dev/null 2>$1

cd /tmp/php53s-module >/dev/null 2>$1

yumdownloader $1

rpm2cpio *.rpm | cpio -idmv

cp -rf /tmp/php53s-module/usr/lib/php/modules/* /opt/php53s/lib >/dev/null 2>$1
cp -rf /tmp/php53s-module/usr/lib64/php/modules/* /opt/php53s/lib >/dev/null 2>$1

cp -rf /tmp/php53s-module/usr/lib/php/zend/* /opt/php53s/zend >/dev/null 2>$1
cp -rf /tmp/php53s-module/usr/lib64/php/zend/* /opt/php53s/zend >/dev/null 2>$1

cp -rf /tmp/php53s-module/etc/php.d/* /opt/php53s/etc/php.d >/dev/null 2>$1




