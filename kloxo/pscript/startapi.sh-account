#!/bin/sh

if [ "$1" != "" ] && [ "$2" != "" ] && [ "$3" == "" ] ; then
	echo "Hi..."
	#startapi.sh --setAPIKey $1 --password $2
	#startapi.sh --setAPIToken $3
else
	echo
	echo "* Format: 'sh $0 <key-link> <key-password> <token>'"
	echo
fi

